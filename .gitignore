node_modules/
dist/
functions/lib/
functions/node_modules/

# Firebase
.firebase/
.firebaserc
firebase-debug.log
firestore-debug.log
ui-debug.log

# Local config — VITE_ vars are public by design, but the project ids are not
# worth committing before the project exists.
.env.local
.env.*.local

.DS_Store
*.tsbuildinfo

# Emulator state kept between `npm start` runs
.emulator-data/
test-results/
playwright-report/
